import type { CurrentlyContent } from "./types";

const currentlyEn: CurrentlyContent = {
  items: [
    "Currently working as a Product Owner at PagBrasil",
    "Diving into the world of AI and building side projects with it",
    "Always looking for challenges and things that push me out of my comfort zone",
  ],
};

export default currentlyEn;
